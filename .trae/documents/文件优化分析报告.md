## 清理范围

### 高优先级文件
1. **Comment.astro** - 移除 80+ 行 `:global(html.dark .*)` 样式
2. **PostCard.astro** - 移除 `dark:hover:text-[var(--primary)]` 等类
3. **PostMeta.astro** - 移除 `dark:text-neutral-400` 等类
4. **DisplaySettings.svelte** - 移除 `dark:bg-[oklch(...)]` 等样式
5. **MainGridLayout.astro** - 移除多处 dark 类
6. **[...slug].astro** - 移除多处 dark 类

### 中优先级文件
7. **Footer.astro** - 移除 `dark:border-white/15` 等类
8. **Navbar.astro** - 移除未使用的 `switchTheme` 函数

## 预期效果
- 减少 CSS 体积约 100+ 行
- 提升代码可维护性
- 所有清理都是安全的（已强制白色模式）

## 执行步骤
1. 逐个文件清理 dark 模式相关代码
2. 每个文件修改后验证语法
3. 全部完成后执行 pnpm build && pnpm preview 测试
4. 更新 PROJECT_STRUCTURE.md 变更日志