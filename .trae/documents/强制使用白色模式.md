禁止用户切换白色和暗色模式，强制使用白色模式：

**修改内容：**

1. **Layout.astro** (第144-170行)

   * 修改内联脚本，强制使用亮色模式

   * 移除 `localStorage.theme` 读取，始终移除 `dark` 类

2. **Navbar.astro** (第54行)

   * 注释掉 `<LightDarkSwitch client:only="svelte" />`，隐藏切换按钮

3. **setting-utils.ts** (第54-61行)

   * 修改 `setTheme` 和 `getStoredTheme` 函数，强制使用亮色模式

这样修改后：

* 页面始终显示白色模式

* 用户看不到主题切换按钮

* 即使用户之前设置了暗色模式，也会被强制覆盖为白色模式

